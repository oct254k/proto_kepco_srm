# PMS·SRM 역할별 메뉴 검증 보고서

- 작성일: 2026-05-11
- 대상 리포지토리: `projects/proto_kepco_srm`
- 기준 문서: `/Users/gsitm/SRM/02_정보구조도.md`, `/Users/gsitm/SRM/07_PMS_SRM_프로토타입_기능보강안.md`
- 실제 구현 기준: `src/lib/menu.ts`, `src/lib/access.ts`, `src/app/**/page.tsx`

## 1. 검증 목적

현재 프로토타입의 **실제 메뉴명**을 역할별 기준값으로 고정하고, 문서상 정보구조와 구현 사이의 차이를 분리해 확인한다.

## 2. 실제 메뉴명 기준선

### 2.1 사업담당자(B)

| 구분 | 실제 메뉴명 | 실제 진입 화면 |
|---|---|---|
| 1 | 대시보드 | `/b/dashboard/` |
| 2 | 견적 | `견적요청 관리` |
| 3 | 발주 | `발주계약요청` |
| 4 | 입찰 | `입찰계획·공고 조회`, `입찰심사` |
| 5 | 마이페이지 | `마이페이지` |

### 2.2 협력업체(V)

| 구분 | 실제 메뉴명 | 실제 진입 화면 |
|---|---|---|
| 1 | 대시보드 | `/v/dashboard/` |
| 2 | 견적 | `견적작성·현황` |
| 3 | 입찰참여 | `입찰 파이프라인`, `입찰·투찰 현황` |
| 4 | 계약·보증 | `계약·보증 관리` |
| 5 | 마이페이지 | `기업정보` |

### 2.3 계약담당자(C)

| 구분 | 실제 메뉴명 | 실제 진입 화면 |
|---|---|---|
| 1 | 대시보드 | `/c/dashboard/` |
| 2 | 발주관리 | `발주계약요청·계획`, `견적요청 관리` |
| 3 | 입찰관리 | `입찰계획·공고` |
| 4 | 참여업체평가 | `평가·심사관리` |
| 5 | 낙찰관리 | `낙찰관리` |
| 6 | 계약관리 | `계약관리` |
| 7 | 마이페이지 | `마이페이지` |

### 2.4 관리자(A)

| 구분 | 실제 메뉴명 | 실제 진입 화면 |
|---|---|---|
| 1 | 사용자관리 | `사용자·협력업체 관리` |
| 2 | 협력업체관리 | `협력업체 승인관리` |
| 3 | 기준정보(품목) | `품목 기준정보` |
| 4 | 시스템환경 | `시스템 환경설정` |

## 3. 문서 대비 판정

| 역할 | 기준 문서 기대 구조 | 실제 구현 판정 | 비고 |
|---|---|---|---|
| 사업담당자(B) | `참여업체평가`가 독립 메뉴, `입찰`은 조회 메뉴 중심 | 부분일치 | `입찰심사`가 `입찰` 하위로 흡수되어 상위 메뉴 수가 6→5로 축소됨 |
| 협력업체(V) | `입찰`, `입찰참여관리`, `계약`이 분리 | 부분일치 | `입찰`+`입찰참여관리` → `입찰참여`, `계약` → `계약·보증`으로 통합됨 |
| 계약담당자(C) | `견적`, `발주`, `입찰`, `계약`이 각각 독립 | 부분일치 | `견적`이 `발주관리` 하위로 편입되고, 상위 명칭이 `발주관리/입찰관리/계약관리`로 운영형 라벨로 바뀜 |
| 관리자(A) | `기준정보 관리`, `시스템 환경설정` 표기 | 부분일치 | 실제 메뉴명은 `기준정보(품목)`, `시스템환경`으로 축약되어 있음 |

## 3.1 Playwright UI 실측

- B: `대시보드 / 견적 / 발주 / 입찰 / 마이페이지`
- V: `대시보드 / 견적 / 입찰참여 / 계약·보증 / 마이페이지`
- C: `대시보드 / 발주관리 / 입찰관리 / 참여업체평가 / 낙찰관리 / 계약관리 / 마이페이지`
- A: `사용자관리 / 협력업체관리 / 기준정보(품목) / 시스템환경`
- 교차 접근 차단: B 역할 상태에서 `/c/contracts/` 직접 진입 시 `403 접근 제한` 확인

## 4. 역할별 접근 검증 결과

| 검증 항목 | 결과 | 근거 |
|---|---|---|
| 메뉴 정의와 기준선 일치 | PASS | `src/lib/menu.ts`를 `node scripts/verify-menu-structure.mjs`로 검증 |
| 메뉴 href별 실제 페이지 존재 | PASS | `src/app/**/page.tsx` 26개 라우트 자동 대조 |
| 기본 랜딩 경로 일치 | PASS | `src/lib/access.ts`의 `DEFAULT_PATHS` 대조 |
| 역할 prefix 접근 제어 일치 | PASS | `/b/`, `/v/`, `/c/`, `/a/` prefix와 메뉴 href 대조 |
| 문서상 상위 메뉴 명칭 완전 일치 | FAIL | 프로토타입은 실제 운영형 메뉴명으로 일부 통합·축약되어 있음 |

## 5. 해석 및 권고

1. **현재 프로토타입의 실제 메뉴 기준선은 구현상 안정적**이다.
   - 메뉴 정의, 기본 진입 경로, 역할별 라우트 연결은 모두 정상이다.
2. **문서와 구현의 차이는 오류라기보다 통합/축약 결정**에 가깝다.
   - 특히 B/V/C 역할은 상위 메뉴를 줄이고 하위 기능을 흡수하는 방향으로 단순화됐다.
3. 이후 리더 문서 산출물에서는 아래 중 하나를 반드시 선택해야 한다.
   - 문서를 현재 프로토타입 기준 메뉴명으로 동기화
   - 또는 프로토타입 메뉴명을 문서 기준으로 되돌려 용어를 일치화
4. 우선순위는 **C 역할 `견적` 독립 여부**, **V 역할 `입찰`/`입찰참여관리` 분리 여부** 확인이다.
   - 두 지점이 역할별 업무 분장 설명에 가장 큰 영향을 준다.

## 6. 자동 검증 명령

```bash
node scripts/verify-menu-structure.mjs
npx tsc --noEmit
```


## 7. 2026-05-11 LNB 구조 재정비 코드 품질 리뷰 추가 메모

### 7.1 이번 리뷰 범위
- 리뷰 대상: `src/components/Sidebar.tsx`, `src/lib/menu.ts`, `src/lib/access.ts`, `src/components/Header.tsx`, `src/components/Breadcrumb.tsx`
- 확인 기준: 역할별 메뉴 config 분리, `single/group` 타입 구조, 공통 렌더링, active route / parent active 처리, 2Depth 노출 정책, 검증 스크립트 신뢰성

### 7.2 유지해야 할 현재 기준
- `src/lib/menu.ts`의 `roleBMenu / roleVMenu / roleCMenu / roleAMenu` 분리는 현재 역할별 LNB 기준선과 일치한다.
- 그룹 메뉴는 현재 `B: 입찰`, `C: 발주관리`만 유지하고, 나머지는 단일 메뉴로 두는 구조가 현재 프로토타입 기준선과 맞는다.
- `single/group` 타입 분리와 breadcrumb 계산 로직은 현재 IA 축약판 기준으로는 합리적이다.
- 문서 대비 차이(B/V/C 메뉴 통합, A 용어 축약)는 신규 결함이라기보다 기존 프로토타입 의사결정의 연장선이다.

### 7.3 코드 품질 이슈

#### 이슈 1. `Sidebar.tsx` 타입 오류
- 현상: `src/components/Sidebar.tsx`에서 `SEARCH_BG`를 사용하지만 상수가 선언되어 있지 않아 TypeScript 에러가 발생한다.
- 확인 결과: `TS2552: Cannot find name 'SEARCH_BG'`
- 영향: 전체 `npx tsc --noEmit` 실패
- 안전한 수정 방향:
  - `const SEARCH_BG = "#F9F9F9";` 추가 또는
  - 해당 속성을 기존 literal 값으로 복원
- 비고: 이 이슈는 `menu.ts` 충돌 이슈와 별개이며, Sidebar 쪽 국소 수정으로 해결 가능하다.

#### 이슈 2. `scripts/verify-menu-structure.mjs`의 false negative
- 현상: 문서상 PASS로 기록된 `node scripts/verify-menu-structure.mjs`가 현재 코드에서는 FAIL을 반환한다.
- 원인: `extractLiteralBlock(menuSource, 'export const roleBMenu', '[', ']')`가 실제 배열 literal이 아니라 타입 표기 `MenuItem[]`의 `[]`를 먼저 잡는다.
- 결과:
  - 역할별 메뉴 정의 비교가 모두 FAIL로 표시됨
  - orphan route 검사도 연쇄적으로 FAIL처럼 보임
- 영향: 메뉴 구조 검증 자동화 결과를 현재 그대로 신뢰할 수 없음
- 안전한 수정 방향:
  - declaration 이후 첫 `=`를 기준으로 literal 시작점을 잡도록 스크립트 수정
  - 또는 TS AST/정규식이 아닌 더 안전한 파서 기반으로 전환

### 7.4 종합 판정
- 메뉴 구조 자체(`src/lib/menu.ts`)는 현재 기준선 유지가 우선이다.
- 즉시 조치가 필요한 것은 `menu.ts` 재충돌 해소가 아니라:
  1. `Sidebar.tsx`의 `SEARCH_BG` 타입 오류 제거
  2. `verify-menu-structure.mjs`의 literal 추출 버그 수정
- 위 두 건이 정리돼야 LNB 재정비 결과를 안정적으로 검증했다고 말할 수 있다.

### 7.5 후속 권고
1. `menu.ts`는 현재 기준을 유지하고 강제 재해석하지 않는다.
2. Sidebar 수정은 UI 정책(2Depth 항상 노출, 단일 메뉴 직접 이동)을 바꾸지 않는 범위에서 최소 수정으로 끝낸다.
3. 검증 스크립트는 타입 주석이 포함된 선언문에서도 정상 동작하도록 먼저 보정한다.
4. 향후 문서 보고서는 “실제 메뉴 구조 기준선”과 “문서 IA 이상형”을 계속 분리해 기록한다.
